# Convenience file for developers.
# Recommended: keep the real config in `config/.env`
#
# Copy to: .env (repo root) ONLY if you need to override values in config/.env
#
# See `config/.env.example` for the full list of supported variables.

